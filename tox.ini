[tox]
isolated_build = True
envlist = py39

[testenv]
deps =
    black
    coverage
    flake8
    pylint
    pytest
commands =
    black simplebeam
    flake8 simplebeam
    pylint simplebeam
    coverage erase
    coverage run --include=simplebeam/* -m pytest -ra
    coverage report -m